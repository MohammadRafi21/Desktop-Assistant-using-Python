# Desktop-Assistant-using-Python

conda create -n assistant(name Of The Environment) python=3.8

conda env list

conda activate assistant(name of the environment)

pip list (to see the version of the python package)

pip install -r requirements.txt